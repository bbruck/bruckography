<%= simple_form_for Work.create do |f| %>
  <%= f.input :name %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.submit "Upload image", class: "btn btn-primary" %>
<% end %>
